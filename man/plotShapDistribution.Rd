% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plotShapDistribution}
\alias{plotShapDistribution}
\title{Plot SHAP Distribution by Model}
\usage{
plotShapDistribution(shap_raw, top_features = NULL, top_n = 15L)
}
\arguments{
\item{shap_raw}{A data.frame of raw SHAP values from
\code{\link{computeFeatureImportance}}.}

\item{top_features}{Character vector of feature names to display.
If \code{NULL}, the top \code{top_n} features are computed
automatically.}

\item{top_n}{Integer. Number of top features to show if
\code{top_features} is \code{NULL} (default 15).}
}
\value{
A \code{ggplot} object.
}
\description{
Displays a jitter + boxplot showing the distribution of SHAP values
for the top features, broken down by model.
}
\examples{
  # Mock SHAP raw data
  mock_shap <- data.frame(
    variable = rep(c("Taxon_A", "Taxon_B"), each = 10),
    contribution = rnorm(20),
    model = rep(c("RF", "XGB"), 10)
  )

  plotShapDistribution(mock_shap, top_n = 2)


}
